{
  "name": "proto-generated",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "package.json",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run gen && npm run build"
  },
  "devDependencies": {
    "glob": "^11.0.3",
    "protoc": "^1.1.3",
    "ts-proto": "^2.7.7"
  },
  "dependencies": {
    "@grpc/grpc-js": "^1.13.4"
  }
}
